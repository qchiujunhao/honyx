# Result

The run summarized five observations across two groups:

- `control`: three observations, arithmetic mean 2.0;
- `treated`: two observations, arithmetic mean 5.0.

These values are recorded in the declared machine-readable output
`results/summary.json` and can be regenerated with the canonical entrypoint.

